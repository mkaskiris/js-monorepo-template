// ESLint is configured at the repo root (eslint.config.js).
// This file exists only so `eslint .` run from within this package
// still works — it re-exports the root config unchanged.
export { default } from '../../eslint.config.js'
